import 'package:flutter/material.dart';

class VetHome extends StatelessWidget {
  const VetHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Home'),
      ),
    );
  }
}